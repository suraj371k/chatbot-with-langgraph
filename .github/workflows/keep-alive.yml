name: Keep Render Alive

on:
    schedule:
        - cron: "*/10 * * * *"
    workflow_dispatch:

jobs:
    ping:
        runs-on: ubuntu-latest
        steps:
            - name: Ping website
              run: curl -sSf https://your-app-name.onrender.com > /dev/null
